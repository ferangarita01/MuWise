// Restrict write access to a user's own folder.
// Users can only write to a path that matches their user ID.
match /users/{userId}/{allPaths=**} {
  allow write: if request.auth != null && request.auth.uid == userId;
}
